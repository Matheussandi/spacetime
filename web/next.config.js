/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'localhost'], // also put the ip of the cell phone
  },
}

module.exports = nextConfig
